{
	"name": "berryman-social-graph",
	"version": "1.0.0",
	"type": "module",
	"description": "A social graph visualization tool for Matt Berryman's work.",
	"scripts": {
		"build": "npm run build:clean && npm run build:compile && npm run build:sri && npm run build:inject",
		"build:clean": "rimraf build",
		"build:compile": "ROOT_DIR=. SRC_DIR=src OUTPUT_DIR=build node build-tools/scripts/build.js",
		"build:sri": "ROOT_DIR=. OUTPUT_DIR=build node build-tools/scripts/generate-sri.js",
		"build:inject": "ROOT_DIR=. OUTPUT_DIR=build node build-tools/scripts/inject-sri.js",
		"build:staging": "npm run build && ROOT_DIR=. OUTPUT_DIR=build FORCE_INJECT=true node build-tools/scripts/inject-staging-banner.js",
		"dev": "ROOT_DIR=. SRC_DIR=src OUTPUT_DIR=build node build-tools/scripts/build-local.js",
		"lint": "biome ci",
		"lint:fix": "biome check --write .",
		"format": "biome format --write ."
	},
	"dependencies": {
		"clean-css": "^5.3.2",
		"html-minifier-terser": "^7.2.0",
		"rimraf": "^5.0.1",
		"terser": "^5.24.0"
	},
	"devDependencies": {
		"@biomejs/biome": "1.9.4",
		"lefthook": "^1.11.13"
	}
}
